FROM python:3.11-slim

WORKDIR /app

# Pre-install heavy dependencies so container startup is fast.
RUN python -m pip install --no-cache-dir pandas "PyJWT[crypto]>=2.8,<3"

CMD ["python", "Milestone_1/replay_dashboard/run_replay_dashboard.py", "--host", "0.0.0.0", "--port", "8775", "--no-browser"]
